---
title:  "[컴퓨터 구조] 컴퓨터 구조와 운영체제 2"
excerpt: "ALU와 제어장치 / 레지스터 / 명령어 사이클과 인터럽트"
categories:
  - ComputerArchitecture
tags:
  - [공부용]

toc: true
toc_label: "List"
toc_icon: "bars"
toc_sticky: true
 
date: 2026-01-02
last_modified_at: 2026-01-02
---

<br>

# <span style="color:rgb(0, 180, 180)">간단히 알아보는 컴퓨터 구조와 운영체제</span>
<span style="font-size: large">해당 포스팅은 **혼자 공부하는 컴퓨터구조+운영체제**를 기반으로 작성하는 글입니다.  
총 15개의 챕터로 이루어져 있으며 적당히 분배하여 포스팅하겠습니다.</span>

## ✔️먼저읽기✔️
**Chapter 4.**  
<u>CPU의 작동 원리</u>로 **ALU와 제어장치, 레지스터, 명령어 사이클과 인터럽트**를 자세히 알아봅시다.

## 참고자료
강민철. **『혼자 공부하는 컴퓨터구조 + 운영체제』**, 한빛미디어(2022년 출간) 

<br>

# Chapter 4
## Arithmetic Logic Unit(산술논리연산장치)
- <u>ALU</u>는 CPU 내부의 **계산(연산)**을 담당합니다.  
- 레지스터로부터 **피연산자**를 받아 연산을 수행합니다.  
- 연산 결과 <u>부가 정보</u>는 **플래그 레지스터(부호, 제로, 오버플로우 등)**를 통해 표현합니다.

![alt text](/assets/images/ALU Diagram.jpg)
<br>
<u>준비물</u>은 **어떻게 연산할지에 대한 제어신호**와 **연산할 데이터 2개**  
<u>출력물</u>은 **단순 결과값을 저장할 레지스터**와 **이를 보안할 플래그 레지스터**  

<u>결과값</u>이 **레지스터에 저장된 이유**는 <u>메모리 접근</u>이 **오래 걸리기 때문**입니다.  

연산 결과의 <u>추가 정보</u>는 **플래그(Flag)**를 통해 표현되고 이 정보들은 **플래그 레지스터**에 임시 저장됩니다.

### 플래그의 종류
<u>플래그</u>는 **부호, 제로, 캐리, 오버플로우, 인터럽트, 슈퍼바이저**가 **포함**됩니다.  

* **부호 (Sign):** 연산 결과의 양수/음수 여부
* **제로 (Zero):** 연산 결과가 0인지 여부
* **캐리 (Carry):** 연산 중 올림수나 빌림수 발생 여부
* **오버플로우 (Overflow):** 표현 가능한 범위를 초과했는지 여부
* **인터럽트 (Interrupt):** 인터럽트 처리가 가능한지 여부
* **슈퍼바이저 (Supervisor):** 커널 모드인지 사용자 모드인지 구분

<div class="notice" style="padding: 0.5em; text-align: center; font-weight: bold;">
  <span style="font-size: medium;">🌠 플래그를 복잡하게 생각할 필요 없이 "추가정보를 표현하는구나"라고 이해하시면 됩니다. </span>
</div>

## Control Unit(제어장치)
- <u>제어신호</u>를 내보내거나 **명령어를 해석**하는 장치입니다.  
[대표적인 제어신호로 **메모리 읽기/쓰기**가 있습니다.]  

- <u>클럭 단위</u>로 **동작**하며 **신호를 발생**시킵니다.  
[명령어/플래그 레지스터의 정보를 받아 해석 후 제어 신호를 생성]

- 생성된 신호는 **ALU와 레지스터**(CPU 내부)로 전달됩니다.  
혹은 **메모리와 입출력장치**(CPU 외부)로 전달되기도 합니다.

![alt text](/assets/images/Control Unit Diagram.jpg)

<br>

<u>클럭</u>은 **모든 부품의 동작 기준**이며 <u>주기에 맞춰</u> **레지스터 간 데이터 이동**, **ALU 의 연산 등**을 수행합니다.  
[명령어마다 클럭 주기가 다름]

<u>제어 버스</u>를 통해 **외부 장치의 제어 신호를 전달받을** 수도 있습니다.

## Register(레지스터)
- CPU 내부의 <u>임시 저장 장치</u>이며 **주요 레지스터는 4가지**입니다.

- <u>프로그램 카운터(PC)</u>는 실행할 **명령어의 위치(주소)**를 <u>저장</u>합니다.  
[다른 말로 **명령어 포인터**이며 **명령이 실행되면 값(주소)을 증가**시킴]

- <u>명령어 레지스터(IR)</u>는 해석할 **명령어**를 <u>저장</u>합니다.  
[제어장치는 해석하고 **제어 신호**를 내보냄]

- <u>메모리 주소 레지스터(MAR)</u>는 읽고 싶은 **주소 값**을 담습니다.  
[CPU가 메모리에 접근할 때 **주소 버스**를 이용]

- <u>메모리 버퍼 레지스터(MBR)</u>는 주고받을 **데이터와 명령어**를 <u>저장</u>합니다.  
[다른 말로 **메모리 데이터 레지스터**이며 **데이터 버스**를 이용]
<br>

### 레지스터의 작동 방식
예시로 **프로그램 카운터(PC)**가 `50번지`를 가리키고 있을 때, 명령어를 가져오는 구체적인 흐름입니다.  

![alt text](/assets/images/Register operation method.webp)

1. <u>메모리 접근</u>을 위해 **MAR(메모리 주소 레지스터)**에게 <u>주소를 넘깁</u>니다.  
2. 제어장치는 <u>Read 신호</u>를 보내고 **50번지 메모리**에 <u>접근</u>합니다.  
3. 읽어온 <u>데이터(1010)</u>를 **MBR(메모리 버퍼 레지스터)**에 <u>저장</u>합니다.  
이때 **PC는 다음 번지**를 가리킵니다. (51번지로 증가)  
4. <u>MBR의 데이터</u>를 **IR(명령어 레지스터)**에게 <u>주소를 넘깁</u>니다.  

이후 **IR**은 명령어를 해석해 **다음 행동을 결정**합니다.  

<div class="notice" style="padding: 0.5em; text-align: center; font-weight: bold;">
  <span style="font-size: medium;">🌠 PC는 순차적으로 번지 수를 저장하지만 `Jump, Call, Ret`과 같은 명령어는 목표 주소로 이동하게 됩니다! </span>
</div>

### 그 외 레지스터들
- **범용 레지스터:** <u>데이터와 주소</u>를 모두 **담을 수 있는 레지스터**입니다.  
- **플래그 레지스터:** <u>연산 결과 또는 CPU 상태</u>의 **부가 정보**를 <u>저장</u>합니다.
- **스택 포인터:** <u>스택의 최상단(Top)</u>를 **가리키는 레지스터**입니다.  
- **베이스 레지스터:** <u>기준 주소</u>에 **특정 값(Offset)을 더한 값을 주소로 지정**할 때 사용되는 <u>기준 주소를 담는 레지스터</u>입니다.  

## 특정 레지스터의 주소 지정 방식
앞 장에 설명되지 않은 <u>프로그램 카운터, 베이스 레지스터, 스택 포인터</u>을 **활용한 주소 지정 방식**입니다.

### 스택 주소 지정 방식
<u>스택</u>은 **후입 선출(LIFO)** 구조입니다.  
<u>스택 포인터</u>는 **데이터의 출입구를 가르키는 레지스터**입니다.  
[즉, push/pop이 일어날 위치를 가르킴]

<u>스택</u>은 **메모리 영역의 일부를 암묵적으로 부여**받은 것이며 <u>스택</u>은 **여러 개 존재**할 수 있습니다.  

![alt text](/assets/images/Stack Pointer.webp)

<br>

### 변위 주소 지정 방식
이 방식은 **프로그램 카운터**나 **베이스 레지스터**를 사용하는 방식으로 나뉩니다.  
<u>변위</u>는 **특정 주소**에서 <u>어느 정도 이동한 위치(Offset)</u>를 의미합니다.

![alt text](/assets/images/Displacement Addressing.webp)

**1. 상대 주소 지정 방식**
- <u>프로그램 카운터</u>의 위치에서 **오퍼랜드(Offset) 값**을 <u>더한 주소를 찾는 방식</u>입니다.  
[분기 명령 등에서 **다음 실행할 명령어의 위치를 지정**할 때 주로 사용]

**2. 베이스 레지스터 주소 지정 방식**
- 기준 주소를 담는 <u>베이스 레지스터</u>에 **오퍼랜드 값**을 <u>더한 주소를 찾는 방식</u>입니다.  
[프로그램을 메모리의 **어느 물리적 위치에 로드해도 코드 수정없이 정상적으로 실행(재배치)**할 수 있음]

## 명령어 사이클과 인터럽트
<u>명령어 사이클</u>은 **CPU가 하나의 명령어를 처리하는 주기**를 의미합니다.  
간혹 이 <u>흐름을 잠시 중단</u>해야 하는 상황을 **인터럽트**라고 하죠.

### 명령어 사이클의 실행 과정
우선 <u>메모리에 저장된 명령어</u> 하나를 CPU의 **명령어 레지스터로 가져**옵니다. => **인출 사이클**  
이후 <u>명령어 레지스터</u>를 **해석**하고 <u>제어 신호</u>를 **발생**시킵니다. => **실행 사이클**  
[일반적으로 인출과 실행 사이클을 반복하며 작동]

일반적이지 않은 경우도 있습니다.   
가져온 명령어가 <u>간접 주소 지정 방식</u>일 경우 **메모리에 한 번 더 접근**해야 합니다.  
[오퍼랜드 필드가 유효 주소의 주소를 가리키므로]  

유효 주소를 찾기 위해 <u>메모리에 한 번 더 접근</u>하는 과정을 **간접 사이클**이라고 합니다.

![alt text](/assets/images/instruction cycle.jpg)

<br>

### 인터럽트의 개념과 종류
인터럽트는 중요한 일이나 문제가 생겼을 때 **하던 일을 잠시 중단하는 용도**로 사용됩니다.  
인터럽트는 발생 원인에 따라 동기와 비동기로 나뉩니다.

**동기 인터럽트 (예외)**  
- CPU가 실행 중인 <u>명령어의 결과</u>로 **오류나 예외 상황**이 발생할 때 일어납니다.  

**비동기 인터럽트 (하드웨어)**  

- 주로 입출력장치(키보드 등)의 <u>외부 신호</u>에 의해 **실행 중인 명령어와 무관**하게 발생합니다.  
[불규칙적이고 느린 외부 장치의 신호를 기다리지 않기 위해 사용됨]

<div class="notice" style="padding: 0.5em; text-align: center; font-weight: bold;">
  <span style="font-size: medium;">🌠 즉, <u>실행 명령어의 독립성</u>에 따라 동기/비동기가 구분됩니다. </span>
</div>

![alt text](/assets/images/Interrupt.jpg)

### 비동기 인터럽트의 처리 순서
1.  입출력장치가 CPU에 <u>인터럽트 요청 신호</u>를 **전달**합니다.
2.  CPU는 실행 사이클이 끝난 후, <u>명령어 인출 전</u> **인터럽트 여부**를 <u>확인</u>합니다.
3.  인터럽트 요청이 오면 <u>인터럽트 플래그</u>를 통해 **수용 가능 여부**를 <u>확인</u>합니다.
4.  인터럽트를 받을 수 있다면 CPU는 <u>현재 작업</u>을 **백업**합니다.
5.  <u>인터럽트 벡터</u>를 **참조**하여 <u>인터럽트 서비스 루틴</u>을 **실행**합니다.
6.  루틴의 실행이 끝나면 백업해 둔 <u>작업을 복구 후</u> **실행 재개**합니다.

<br>

**인터럽트의 핵심 키워드는?**  
<br>
**인터럽트 요청 신호** - 입출력장치가 <u>인터럽트 발생</u>을 알리기 위해 보내는 **신호**입니다.  
[인터럽트는 CPU 명령어를 가로챌 때 발생하며 주변 장치가 직접 발생시키는 것이 아님]

**인터럽트 플래그** - 하드웨어 인터럽트의 <u>수용 여부를 결정</u>하는 **플래그**입니다.

**인터럽트 벡터** - <u>인터럽트 서비스 루틴(ISR)</u>의 **시작 주소**를 <u>구분하기 위한 정보</u>입니다.  
[요청 대상에게 데이터 버스를 통해 벡터를 전달받음]

**인터럽트 서비스 루틴 (ISR)** - 특정 인터럽트 발생 시 <u>약속된 작업을 수행</u>하는 **특수한 함수**입니다.  

<br>

**인터럽트 시 기존 명령어는?**  
CPU는 <u>기존 실행할 명령어</u>를 **스택 영역**에 <u>백업</u>합니다.  
이후 <u>프로그램 카운터</u>에 **ISA 시작 주소**를 <u>갱신</u>합니다.

![alt text](/assets/images/Interrupt Service Routine.jpg)

<hr>

<div class="notice--info" style="font-weight: normal;">
  <span style="font-size: medium;">✨개인 공부 글입니다! 언제든지 질문/지적 해주세요!✨</span>
</div>